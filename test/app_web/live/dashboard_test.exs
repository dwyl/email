defmodule AppWeb.DashboardTest do
  use AppWeb.ChannelCase

  # setup do
  #   {:ok, _, socket} =
  #     socket("user_id", %{some: :assign})
  #     # |> subscribe_and_join(RoomChannel, "live")
  #
  #   {:ok, socket: socket}
  # end

  # describe "AppWeb.Dashboard > " do
  #   test "handle_event/3", %{socket: socket} do
  #     IO.inspect(socket, label: "socket 6")
  #     # {:noreply, new_state} = AppWeb.Dashboard.handle_event("sent", "", socket)
  #     # IO.inspect(new_state, label: "new_state 7")
  #     # conn = get(conn, Routes.sent_path(conn, :index))
  #     # assert html_response(conn, 200) =~ "Listing Sent"
  #   end
  # end
end
